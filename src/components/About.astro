---
// Define reusable stats data
const stats = [
    {
        value: "500+",
        label: "Companies Satisfied",
    },
    {
        value: "100%",
        label: "Personal Attention",
    },
    {
        value: "24/7",
        label: "Technical Support",
    },
    {
        value: "10+",
        label: "Years of Experience",
    },
];
---

<article class="w-full px-4 py-24 bg-gray-50 lg:px-8">
    <div class="w-full max-w-7xl mx-auto flex flex-col items-center justify-center gap-12">
        <section class="reveal w-full text-(--color-primary) text-center max-w-3xl mx-auto">
            <h2 class="text-3xl font-bold mb-4 md:text-4xl">Our Achievements</h2>
            <p class="text-(--color-dark)/70 text-lg">
                This section showcases our success metrics and commitment to
                excellence.
            </p>
        </section>
        <div
            class="w-full flex flex-col gap-12 lg:flex-row-reverse lg:items-center lg:justify-between"
        >
            <section
                class="reveal w-full aspect-video border rounded-2xl shadow-lg overflow-hidden lg:flex-1 lg:max-w-xl"
            >
                <video
                    class="w-full h-full object-cover"
                    src="/demo-video.mp4"
                    controls
                    muted
                    autoplay
                    loop
                >
                    Your browser does not support the video tag.
                </video>
            </section>
            <div
                class="reveal w-full grid grid-cols-2 gap-6 lg:grid-cols-2 lg:flex-1"
            >
                {
                    stats.map((stat) => (
                        <div class="flex flex-col items-center justify-center border border-gray-400/30 bg-white rounded-2xl p-8 hover:shadow-xl hover:scale-105 transition-all duration-300 gap-2">
                            <span class="text-4xl text-(--color-primary) font-bold md:text-5xl">
                                {stat.value}
                            </span>
                            <p class="text-(--color-dark)/80 font-medium">
                                {stat.label}
                            </p>
                        </div>
                    ))
                }
            </div>
        </div>
    </div>
</article>
